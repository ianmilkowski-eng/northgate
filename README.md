# Northgate

The one-page version of my Northgate submission for Straterai, Stage 2. It's all in `index.html`, served with GitHub Pages. Press P on the page to present the deck.

The code lives in the other two repos: `northgate-recovery` is the system and `northgate-harness` is how I built it.
